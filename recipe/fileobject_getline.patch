--- Objects/fileobject.c.orig	2016-07-02 08:50:48.000000000 -0500
+++ Objects/fileobject.c	2016-07-02 08:51:08.000000000 -0500
@@ -417,7 +417,7 @@
 */
 
 static object *
-getline(f, n)
+_getline(f, n)
 	fileobject *f;
 	int n;
 {
@@ -538,7 +538,7 @@
 	}
 	if (((fileobject*)f)->f_fp == NULL)
 		return err_closed();
-	return getline((fileobject *)f, n);
+	return _getline((fileobject *)f, n);
 }
 
 /* Python method */
@@ -563,7 +563,7 @@
 			n = 0;
 	}
 
-	return getline(f, n);
+	return _getline(f, n);
 }
 
 static object *
@@ -581,7 +581,7 @@
 	if ((list = newlistobject(0)) == NULL)
 		return NULL;
 	for (;;) {
-		line = getline(f, 0);
+		line = _getline(f, 0);
 		if (line != NULL && getstringsize(line) == 0) {
 			DECREF(line);
 			break;
